# TODO

## Refactoring / Rationalization

- revision the gui user experience of the user actross all the user interface, suggest improvements


## make the bar determinate (actual % from script output), which would require adding progress markers to the .ps1 scripts and parsing them in the IPC layer. That would make the bars genuinely useful

# encript vm-data file


# is it possible to display in the vm detail the list of the program installed with provision


# on a new vm, install fedora 3.3.2, check if the user is warned that a previous version is already present. also check if he s displayed with what he has to do after isntalling, for ex, maintaing...


# openssl if the install fails, how do we roll back? should we give the user the possibility to roll back?

# in case of script error, give the user to rollback, at least for openssl

# provision claude code

# review and rationalize privison-full and provision-script, then try to fix the provision with paused/stopped vm

# eclipse ide use a drop down list for the version

# update the docs and readme see if to attach new screenshots